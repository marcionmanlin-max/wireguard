# IonMan DNS — Database / environment credentials
# Copy this file to config/db.env and fill in your values.
# config/db.env is gitignored and must NEVER be committed.

IONMAN_DB_HOST=localhost
IONMAN_DB_USER=your_db_user
IONMAN_DB_PASS=your_db_password
IONMAN_DB_NAME=ionman_dns
